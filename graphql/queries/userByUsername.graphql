query UserByUsername($username: String!) {
  userByUsername(username: $username) {
    errors {
      field
      message
    }
    user {
      id
      verified
      email
      username
      token_version
      first_name
      last_name
      bio
      country
      created_at
      updated_at
    }
  }
}
