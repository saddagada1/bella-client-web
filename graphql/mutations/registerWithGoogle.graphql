mutation RegisterWithGoogle($username: String!) {
  registerWithGoogle(username: $username) {
    errors {
      field
      message
    }
    user {
      id
      verified
      email
      username
      token_version
      first_name
      last_name
      bio
      country
      created_at
      updated_at
    }
    auth {
      access_token
      expires_in
    }
  }
}
