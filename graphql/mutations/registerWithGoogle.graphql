mutation RegisterWithGoogle($username: String!) {
  registerWithGoogle(username: $username) {
    errors {
      field
      message
    }
    user {
      id
      verified
      email
      username
      created_at
      updated_at
    }
    auth {
      access_token
      expires_in
    }
  }
}
